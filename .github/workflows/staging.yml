name: Staging Deployment

on:
  push:
    branches: [develop, 'claude/**']
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - preview

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-backend:
    name: Build Backend Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=staging-
            type=raw,value=staging-latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  build-frontend:
    name: Build Frontend Assets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ./web

      - name: Build frontend
        run: npm run build
        working-directory: ./web
        env:
          NODE_ENV: production
          VITE_API_BASE_URL: ${{ secrets.STAGING_API_URL || 'https://staging-api.example.com' }}

      - name: Upload frontend artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-staging
          path: web/dist
          retention-days: 30

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [build-backend, build-frontend]
    environment:
      name: staging
      url: https://staging.example.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download frontend artifacts
        uses: actions/download-artifact@v4
        with:
          name: frontend-staging
          path: ./frontend-dist

      - name: Deploy to Staging (Placeholder)
        run: |
          echo "🚀 Deploying to staging environment..."
          echo "Backend image: ${{ needs.build-backend.outputs.image-tag }}"
          echo "Frontend artifacts ready in ./frontend-dist"
          echo ""
          echo "⚠️  DEPLOYMENT CONFIGURATION NEEDED:"
          echo "-----------------------------------"
          echo "Please configure one of the following deployment methods:"
          echo ""
          echo "1. Kubernetes Deployment:"
          echo "   - Set KUBE_CONFIG secret with your kubeconfig"
          echo "   - Update deployment manifests in deployments/k8s/"
          echo "   - Uncomment kubectl commands below"
          echo ""
          echo "2. Docker Compose Deployment:"
          echo "   - Set SSH_PRIVATE_KEY secret for server access"
          echo "   - Set STAGING_HOST secret with server address"
          echo "   - Uncomment docker-compose deployment below"
          echo ""
          echo "3. Cloud Platform (AWS/GCP/Azure):"
          echo "   - Set appropriate cloud credentials"
          echo "   - Configure platform-specific deployment steps"
          echo ""
          echo "4. Container Registry Info:"
          echo "   - Registry: ${{ env.REGISTRY }}"
          echo "   - Image: ${{ needs.build-backend.outputs.image-tag }}"

      # Kubernetes Deployment (Uncomment when ready)
      # - name: Set up kubectl
      #   uses: azure/setup-kubectl@v3
      #
      # - name: Configure kubectl
      #   run: |
      #     mkdir -p $HOME/.kube
      #     echo "${{ secrets.KUBE_CONFIG }}" > $HOME/.kube/config
      #
      # - name: Deploy to Kubernetes
      #   run: |
      #     kubectl set image deployment/intelligent-workflows-api \
      #       api=${{ needs.build-backend.outputs.image-tag }} \
      #       -n staging
      #     kubectl rollout status deployment/intelligent-workflows-api -n staging

      # Docker Compose Deployment (Uncomment when ready)
      # - name: Deploy via Docker Compose
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.STAGING_HOST }}
      #     username: ${{ secrets.STAGING_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /opt/intelligent-workflows
      #       docker-compose pull
      #       docker-compose up -d
      #       docker-compose ps

      # AWS ECS Deployment (Uncomment when ready)
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-east-1
      #
      # - name: Deploy to ECS
      #   run: |
      #     aws ecs update-service \
      #       --cluster staging \
      #       --service intelligent-workflows \
      #       --force-new-deployment

  smoke-tests:
    name: Run Smoke Tests
    runs-on: ubuntu-latest
    needs: [deploy-staging]
    if: success()

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run health checks
        run: |
          echo "🔍 Running smoke tests..."
          echo ""
          echo "⚠️  Configure these health check endpoints:"
          echo "- API_URL: ${{ secrets.STAGING_API_URL || 'https://staging-api.example.com' }}"
          echo "- WEB_URL: ${{ secrets.STAGING_WEB_URL || 'https://staging.example.com' }}"
          echo ""
          echo "Uncomment health check commands below once endpoints are configured"

      # - name: Check API Health
      #   run: |
      #     response=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.STAGING_API_URL }}/health)
      #     if [ $response -eq 200 ]; then
      #       echo "✅ API health check passed"
      #     else
      #       echo "❌ API health check failed with status $response"
      #       exit 1
      #     fi

      # - name: Check Frontend
      #   run: |
      #     response=$(curl -s -o /dev/null -w "%{http_code}" ${{ secrets.STAGING_WEB_URL }})
      #     if [ $response -eq 200 ]; then
      #       echo "✅ Frontend health check passed"
      #     else
      #       echo "❌ Frontend health check failed with status $response"
      #       exit 1
      #     fi

  notify:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [deploy-staging, smoke-tests]
    if: always()

    steps:
      - name: Create deployment summary
        run: |
          echo "## Staging Deployment Summary 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Actor:** @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.deploy-staging.result }}" == "success" ]; then
            echo "✅ Deployment: **SUCCESS**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Deployment: **FAILED**" >> $GITHUB_STEP_SUMMARY
          fi

          if [ "${{ needs.smoke-tests.result }}" == "success" ]; then
            echo "✅ Smoke Tests: **PASSED**" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.smoke-tests.result }}" == "skipped" ]; then
            echo "⏭️ Smoke Tests: **SKIPPED**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Smoke Tests: **FAILED**" >> $GITHUB_STEP_SUMMARY
          fi

      # Slack Notification (Uncomment when ready)
      # - name: Notify Slack
      #   uses: slackapi/slack-github-action@v1
      #   if: always()
      #   with:
      #     webhook: ${{ secrets.SLACK_WEBHOOK }}
      #     payload: |
      #       {
      #         "text": "Staging deployment ${{ needs.deploy-staging.result }} for ${{ github.repository }}",
      #         "blocks": [
      #           {
      #             "type": "section",
      #             "text": {
      #               "type": "mrkdwn",
      #               "text": "*Staging Deployment ${{ needs.deploy-staging.result }}*\n*Repository:* ${{ github.repository }}\n*Branch:* ${{ github.ref_name }}\n*Commit:* ${{ github.sha }}\n*Actor:* ${{ github.actor }}"
      #             }
      #           }
      #         ]
      #       }
